<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserlogonMapper">
    <!--    public Account getUser(Long id); -->
    <select id="getUserlogon" resultType="com.example.demo.entity.Userlogon">
        select * from  userlogon where  id=#{id}
    </select>
</mapper>